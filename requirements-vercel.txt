# Vibespan.ai - Vercel Optimized Requirements
# Minimal dependencies for production deployment

# Core Framework
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0

# Data Processing (minimal)
pandas==2.1.4
numpy==1.24.4

# Security
cryptography==41.0.8

# HTTP & Environment
requests==2.31.0
python-dotenv==1.0.0

# Optional LLM (only if API keys provided)
langchain==0.1.0
langchain-openai==0.1.0
langchain-anthropic==0.1.0
langchain-core==0.1.0
