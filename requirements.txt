# Vibespan.ai - Minimal Vercel Requirements
# Optimized for Python 3.12+ and Vercel deployment

# Core Framework (minimal versions)
fastapi>=0.100.0
uvicorn>=0.20.0
pydantic>=2.0.0

# Environment
python-dotenv>=1.0.0

# HTTP
requests>=2.25.0

# Data Processing (optional - only if needed)
pandas>=1.5.0
numpy>=1.20.0

# Security (optional - only if needed)
cryptography>=3.0.0